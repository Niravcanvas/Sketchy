node_modules/
.next/
dist/
coverage/
.env
*.log
.DS_Store
.eslintcache
tsconfig.tsbuildinfo

# Playwright artifacts
test-results/
playwright-report/

# Local agent working dir (settings, transient agent worktrees) — never commit
.claude/

# Archived internal working docs (roadmap + pre-deploy audits) — kept on disk, out of git
mess/

backup/

# Generated OpenAPI spec (CI-only)
openapi-live.json

# Real deploy secrets/certs (deploy/RUNBOOK.md, deploy/Caddyfile) — never commit;
# deploy/.env.prod.example and deploy/livekit.yaml are templates and stay tracked.
deploy/.env.prod
deploy/certs/
